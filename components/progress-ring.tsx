import type {UILanguage} from '@/lib/schema';
import {t} from '@/lib/i18n';
export function ProgressRing({done,total,language}:{done:number;total:number;language:UILanguage}){const percent=total?Math.round(done/total*100):0;return <div className="progress-box" role="progressbar" aria-label={t(language,'progress')} aria-valuenow={done} aria-valuemin={0} aria-valuemax={total}><div className="progress-dial"><svg viewBox="0 0 52 52" aria-hidden="true"><circle cx="26" cy="26" r="22" fill="none" stroke="#dce5ef" strokeWidth="4"/><circle cx="26" cy="26" r="22" fill="none" stroke="#174de1" strokeWidth="4" strokeDasharray="138.23" strokeDashoffset={138.23*(1-percent/100)} transform="rotate(-90 26 26)" strokeLinecap="round"/></svg><span>{percent}%</span></div><div><strong>{done} / {total} {t(language,'completed')}</strong><p>{Math.ceil((total-done)*.25)} {t(language,'remaining')}</p></div></div>;}
