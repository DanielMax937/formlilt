# FillFlow build log

All times Asia/Shanghai. Built in the Codex task with GPT-6 Astra. Entries distinguish implementation, automated evidence and unverified release requirements.

## 2026-09-16 · Preflight / decisions

- Read SPEC.md in full. Workspace initially contained only the spec; no existing app or Git history.
- User override: agent-im is the initial provider instead of Ark. Ark console/key checks are therefore replaced with inspection of the local agent-im implementation and a live image+JSON request. Real configured model: `codex-login/gpt-6-astra`; base URL `http://127.0.0.1:3300/v1`; placeholder token `agent-im-local` is not an account secret.
- Agent-im health returned 200. It has no `/v1/models` route; model format and default verified from its source/tests. Image preflight pending.
- Keep Next.js 15, Node runtime and Vercel as explicitly specified. Sites' default Vinext/Cloudflare starter and hosting are inapplicable to this user-selected architecture. Apply its accessibility/visual principles only.
- Privacy: no FillFlow database. Browser stores progress and original files. Agent-im/provider may retain its own sessions; disclose this accurately. Demo flow must work without model calls.
- Original quota design required shared Redis in production. The owner superseded this on 2026-09-17: deploy to Vercel without Redis using explicit per-instance memory limits.
- Live preflight passed: HTTP 200 in 53.245s, model correctly returned `FILLFLOW VISION CHECK` and `Full name:` from the synthetic JPEG with JSON-object response mode. No user document was sent. This already exceeds the target latency; do not claim p95 compliance.

## 2026-09-16 01:10 · T0 complete
- Initialized Next.js 15.5.25 / React 19 / Tailwind 4, strict TypeScript, Vitest and Playwright. Added validated server-only environment configuration, README and ignored local env.
- Acceptance: `pnpm test` 1/1 passed; `pnpm build` passed (home and not-found prerendered). No ignored type/build errors.
- Registry mirror was unavailable; installed from the official npm registry. Local server port is 3050.

## 2026-09-16 01:10 · T1 complete
- Added bounded zod models for fields, schemas, answers, turns, finalize and browser sessions. Validate unique IDs, section membership, anchor bounds, options, cyclic dependencies, signature references and 30 KB schema limit.
- Acceptance: `pnpm test` 4/4 passed, including invalid models and invalid API inputs.
- Strict JSON-schema image preflight also passed (HTTP 200), but took 137.896s versus 53.245s for JSON-object mode. Initial agent-im default remains JSON mode with application-level zod validation and one repair retry; full demo extraction timing remains to be measured.

## 2026-09-16 01:15 · T2 complete
- Server uses unpdf text coordinates in displayed top-left page space, plus pdf-lib widget rectangles and option lists. Photos use the scan path. Added client PDF/JPEG/PNG/HEIC rendering, 1600px bound, worker/font asset copy at install.
- Three complete two-page demo forms: IRS text items [122,213], 25 widgets; VA [117,40], 48 widgets; Dublin school [103,69], 0 widgets. All pages classified text. JPEG fixture classified scan.
- Acceptance: `pnpm test` 9/9 passed; `pnpm typecheck` passed. Six sample page images rendered with Poppler at <=1600px. Client rendering will additionally be exercised in browser e2e.
- Real documents caught library API changes (unpdf cleanup through loadingTask and viewport rectangle conversion); corrected against installed types. Rejected encrypted/oversize source samples; school PDF consists of the complete two-page medical form extracted from a larger packet, with source attribution.

## 2026-09-16 01:30 · T3 verification in progress
- Implemented provider switching, SDK generateObject + zod, one repair retry, grounded labels, bounded multipart input, safe errors and extract route.
- Fixed two real agent-im compatibility issues: the compatible SDK's JSON-object mode drops response schema, so the schema is explicitly supplied in the system prompt; agent-im uses Ajv2020, so strict-mode requests convert draft-07 tuple items to prefixItems and remove the draft declaration.
- Synthetic photo full FormSchema accepted in 64.471s with codex-login/gpt-5.6-luna, JSON mode, 1/1 field. Full demo extraction requests still running. No claim of latency target compliance.
- Current automated acceptance: 17/17 unit tests and typecheck passed. Raw model outputs are not logged by routes; fixture-only verification records structured counts and timing.
- Full-schema requests for the three demos timed out at the 300s local test bound. Changed only the model wire representation to compact indexed rows: the server assembles the specified FormSchema from trusted text/widget references. This reduces duplicated labels/coordinates and makes those references exact by construction. New live checks are running. T3 remains open until their result is inspected; prepare subsequent deterministic components while these external checks run, without marking their acceptance complete.

## 2026-09-16 01:40 · T3 functional acceptance complete; latency gate open
- Real extraction succeeded for all requested fixtures after compact source-reference adaptation: address 31 fields / 25 of 25 native fields / 31 matching labels / 186.258s (Luna); insurance 42 fields / 42 of 42 native fields (48 widgets) / 42 matching labels / 129.171s (Astra); school medical 103 fields / no native fields / 103 matching labels / 268.385s (Astra). Photo full-schema test: 1 field / 64.471s (Luna).
- Source visual inventory: address has 25 native inputs + 3 signature lines + 3 dates = 31; insurance 42 unique inputs including signature; school includes contact fields, 20 health checkboxes, 40 medication table cells and signature details. Extracted field counts meet >=90% inventory coverage. Labels are resolved from numbered text items, so text reference match is 100%.
- Existing API unit tests / grounding / repair tests passed. Default remains user-selected agent-im with Astra; raised development request timeout to 300s so tested complex forms can complete. No fabricated success on timeout.
- Release limit: samples take 129–268s, so p95 <20s is NOT met. These local timings are not Vercel-region benchmarks. Runtime for public deployment needs a reachable fast provider; loopback agent-im is not reachable from Vercel. Strict JSON mode full-demo comparison will be recorded separately.

## 2026-09-16 01:41 · T4 complete
- Added repeatable precompute script (live model or validated fixture results), demo manifest and read-only demo API. All three schemas are validated before publishing any file.
- Acceptance: three schemas exist and pass FormSchema validation; sizes 12.8 KB / 16.5 KB / 26.6 KB, all <30 KB. Demo API tests 4/4 passed; typecheck passed. Demo route has no LLM or limiter dependency.

## 2026-09-16 01:43 · T5 complete
- Built responsive homepage with real upload rendering/extraction, three demo cards, saved-session resume and clear-all. Added browser IndexedDB originals, validated localStorage sessions, language switch and four UI dictionaries.
- Acceptance: Chromium e2e clicked the insurance demo, opened its persisted form, refreshed successfully and made zero extract calls: 1/1 passed in 7.3s. Home HTTP 200; preview handed to Codex (queued by app). Typecheck passed.
- The first workspace slice displays the extracted form and sections. Guided question controls follow in T6–T8. No private file or answer is sent in analytics events.

## 2026-09-16 01:44 · T6 complete
- Deterministic section-order routing for answer/skip/back/jump, recursive dependency visibility, inactive-answer pruning and unknown-field rejection. Skipped questions stay available for review instead of causing a loop.
- Acceptance: next-field tests cover branching, revisiting, completion and clearing stale conditional answers; 3/3 passed.

## 2026-09-16 01:51 · T7 complete
- Added streamed turn API with deterministic decisions, localized validation, source-bound explanations, optional model translation and explicit translation confirmation. Browser hook persists optimistic local changes and aborts stale requests.
- Working question workspace includes section todo status, revisiting answers and a live progress ring. Routine turns require no model call; model output cannot change which field comes next.
- Acceptance: turn API tests 3/3 passed; Chromium e2e answered three questions, skipped one, went back, jumped to a prior answer and restored it after refresh: 1/1 passed in 8.3s. Typecheck passed.
- Strict-mode comparison: all three full-demo attempts failed (two optional-property schema requirements from Codex's strict validator; one upstream WebSocket disconnect). JSON mode passed all three; leave LLM_STRUCTURED_OUTPUT=false. Strict mode is not the production recommendation for this agent-im adapter.

## 2026-09-16 02:02 · T8 complete
- Added date/number/email/telephone/select/multiselect/boolean controls, responsive question layout, section todo navigation and progress ring. Keyboard focus follows the current input; reduced motion is respected.
- Visual check caught off-screen status text expanding page height and a Tailwind class-name collision; corrected positioning and progress class naming.
- Mobile WebKit exposed File-object IndexedDB transaction failures. Store ArrayBuffer plus metadata instead, while preserving compatibility with existing File entries. All demo downloads still use the original bytes.
- Acceptance: typed controls, yes/no dependency switching, keyboard submit and no horizontal overflow pass on Chromium and iPhone/WebKit. Screenshots saved in launch/. Typecheck passed.

## 2026-09-16 02:02 · T9 complete
- Type/constraint validation reports inline, localized errors and keeps the current question. Regex checks use a linear-time engine. Explanations cite supplied form help; missing help says the source does not explain it.
- Acceptance: 4/4 validation unit tests (calendar/leap dates, email, phone, numeric bounds, enums, length, adversarial regex); validation e2e passes in Chromium and mobile WebKit. Combined T8/T9 browser acceptance: 4/4 passed in 13.0s.

## 2026-09-16 · T10 implemented; physical-device acceptance open
- Added opt-in question/error speech synthesis and microphone capture, editable transcripts, unsupported-browser feedback, and cancellation when moving to another field. No audio upload endpoint exists in FillFlow; browser speech providers may process recognition remotely, which will be disclosed in About.
- Acceptance: typecheck passed; Chromium and mobile WebKit integration tests exercise recognition callbacks and persisted speech toggle. Physical Chrome microphone and iPhone Safari listening/voice quality require real-device acceptance; automated mocked recognition is not evidence of that manual gate.

## 2026-09-16 · T11 complete
- Added transparent PNG signature drawing with pointer capture (mouse/touch/pen), clear action, keyboard typed signature, signer full name, and auto-filled linked date. Signing does not overwrite an existing date or another person's fields.
- Acceptance: 32/32 unit tests passed, including attribution/date rules and all existing route tests. Four signature e2e cases passed on Chromium and mobile WebKit (typed and pointer paths); typecheck passed. Real stylus hardware remains a manual device check.

## 2026-09-16 · T12 complete
- Added review route with all active sections, inline typed/signature editing, missing required answers, original-page preview, lock option (off by default), JSON export and copyable text summary. PDF download is wired to the T13 endpoint.
- Acceptance: typecheck and 2/2 Chromium/mobile WebKit e2e passed. Verified inline edits survive reload, missing fields remain highlighted, original pages render, and invalid forms cannot start export.

## 2026-09-16 · T13 complete
- Implemented native text/checkbox/radio/select fields, transparent attributed signatures, text-label/blank-line placement, table/scan bounds, photo-to-PDF, embedded Latin/CJK/Arabic fonts, optional flattening and finalize API. Original page count/dimensions, source labels, native field names and all required answers are checked again before export.
- Export rejects overflow rather than truncating an answer. Summary uses a 1.8s model deadline with a source-based fallback; known demos use the deterministic summary and make no model calls. Known-demo exemption compares a complete parsed-schema hash, not a client slug.
- Acceptance: three real forms download and render in Chromium and mobile WebKit (6 full flows passed across the completed runs). 42/42 full unit tests passed, plus 4 new rotation cases; focused fill tests 10/10 passed. Native values, optional flattening, Unicode values, photos and rotations 0/90/180/270 are covered.
- Poppler visual review found and fixed address clipping, signature widget removal without an appearance, and CJK glyph corruption. CFF subsetting was not portable. Converted official Noto CJK variable TrueType to a static 400-weight font and aligned glyf records to 2 bytes to match fontkit's short-loca subsets. Chinese 陈美玲, Japanese 山田花子 and Arabic أحمد علي now render visibly; image/scan outputs open and fit their specified bounds. Rebuild script and OFL notices included.
- Browser end-to-end checks also exposed a localhost/127.0.0.1 origin mismatch and a model language name "English" versus "en". Fixed host-aware same-origin checks and language normalization, with regression tests; full-flow tests now fail on API HTTP errors as well as JS errors.
- Remaining release gates are not implied by these results: physical phone photo, real iOS audio, VoiceOver, and opening outputs in macOS Preview/Adobe Reader still need explicit verification. Placement based on scan/table bounds must be checked against the PDF before external submission.

## 2026-09-16 · T14 complete
- Added oversized PNG-header checks before decompression, reserved-ID rejection, suppressed SDK error logging that could otherwise include provider data, and configurable quota wording. Existing routes enforce bounded bodies, magic bytes, page limits, source validation and no-store/no-referrer headers.
- Acceptance: typecheck and 52/52 tests passed. Four sequential valid upload requests produce [200,200,200,429] and only three model calls; turn 121 is denied; window expiry works; production without Redis fails closed. Forged files, 16-page PDFs, oversized PNG dimensions and oversized bodies are rejected. Exact known-demo schemas receive the documented no-model/no-upload-quota path.

## T15 — 2026-09-16 08:04 CST — privacy, analytics and accessibility

- Implemented four-language About/privacy/model/license information, MIT license, explicit opt-in Vercel Analytics with query/session identifiers removed and no answers in event payloads.
- Added cached, agent-im-generated Chinese/Spanish/Japanese questions for all 176 demo fields. Ordinary demo questions need no runtime model call. Corrected the address form's first three applicability questions after reviewing their meaning.
- Improved contrast, heading hierarchy, signature image restoration, deterministic summary coverage, translation confirmation and source-grounded wording tests. Applied consistent Prettier formatting across the source.
- Public name changed from the spec codename FillFlow to **FormLilt**: existing same-category products found at fillflow.ai and fillflow.co; exact Product Hunt/web search returned no matching FormLilt product; npm registry and .com RDAP returned 404. This is a preliminary availability check, not a trademark clearance or domain purchase. Internal browser storage keys remain compatible.
- Acceptance: `pnpm test` **56/56**, `pnpm typecheck`, `pnpm build` pass. Full production-mode Playwright run **27/28**; final WebKit keyboard test passed after using Safari's Option+Tab (all-controls navigation), yielding all **28 cases verified** across Chromium and mobile WebKit. All three real demo flows produce readable 2-page downloads. Axe reports zero WCAG A/AA violations on home/workspace/signature/review/About in both engines. Keyboard-only address flow signs and downloads in both engines.
- Mobile Lighthouse: **Performance 97, Accessibility 100**, report `tmp/lighthouse-mobile.report.html`, portable metrics `launch/quality-metrics.json`. Preview visually shows the Chinese name and correctly placed school-form answers. Poppler and browser PDF.js export rendering were verified in T13.
- **Release gate remains open**: §7 cannot honestly be fully checked. Local agent-im extraction is still 64–268 seconds (fails <20s target); physical iPhone audio/VoiceOver, Adobe Reader, a real phone photo and five additional manual PDF reviews remain pending. Additional public-form checks are running. T16 can proceed as a clearly identified demo deployment while these gates stay visible; this does not constitute launch acceptance.

## T16 — 2026-09-16 08:27 CST — Vercel demo deployment

- Created MIT source repository https://github.com/DanielMax937/formlilt and a new Vercel project without touching existing projects. Production preview: **https://formlilt.vercel.app**. Vercel build passed (Next 15.5.25, Node 24, functions hkg1, maxDuration 60).
- Vercel defaults to demo-only mode. Server rejects uploads/unknown schemas; checked-in demos can be filled, signed and downloaded without a model. UI explicitly states that live uploads/translation are disabled and answers are used as written. Local agent-im mode remains available.
- Added conservative request-size checks and documented the 4.5 MB Vercel transport constraint versus local 10 MB originals. Added a one-click deployment button, server-only provider/Redis setup instructions, privacy and release limitations, public support URL and app icon.
- Confirmed team plan through Vercel API: **Hobby**. No paid upgrade or add-on was requested. Free Web Analytics page views enabled; custom events require Pro and remain off behind a separate flag. Hobby's free resource limits apply; runtime model spend is zero in preview.
- Acceptance: unit tests **59/59**, typecheck passed, Vercel production build passed. Production mobile WebKit completed address-form questions/signature/review/PDF download; cached question switching passed in both engines. Remaining full remote runs were affected by slow/stalled cross-border resource loading; original failed traces retained locally. Explicit existing HTTP proxy + HTTP/1.1 API check returns **200 in 1.1s**. Production upload endpoint returns **503 demoMode** as intended. More remote flow verification continues after the preview deployment.
- Extra real-form tests all remain failed: W-9 (6 pages/23 widgets), Berlin registration (German, 1 page/53 widgets) and French Cerfa 13750 (1 page/no widgets) each timed out at 300s through agent-im Luna. Encrypted HK form was rejected. Local agent-im health still returns 200. Evidence: `launch/extra-form-results.json`; no fabricated passes. No further benchmark requests are queued.
- Release state is tracked in `QUALITY.md`. Full live-upload deployment, <20s extraction and physical-device/PDF-reader gates remain open; the hosted site is a usable, explicitly labeled preview.

## T17 — 2026-09-16 08:40 CST — optional local profile suggestions

- Chose S2. Homepage opt-in stores full name, current address and an identity-document number in this browser only. Matching empty fields offer an explicit “Use saved detail” button. No automatic answer or submission; relatives' names, first/last-name splits, old addresses and policy numbers are excluded. The ID suggestion is masked until accepted. Clear-all removes the profile.
- Added four-language controls/privacy copy and bounded Zod validation. No new backend, account or analytics payload.
- Acceptance: **62/62 unit tests**, strict typecheck and production build pass. Profile save/suggest/refresh/clear flow passes on Chrome and mobile WebKit. Axe + cached-language checks pass on both engines (six applicable E2E cases verified). Initial dev-mode runs were invalidated by cold compiles; final checks used production mode. Mobile clear-all test uses the visible homepage footer, because the desktop About shortcut is hidden on narrow screens.
- Production follow-up: address download now passes in both engines. Insurance/school browser tests still time out waiting on transfer, while Vercel logs show successful HTTP 200 exports. A direct insurance request spent ~89s before receiving the first byte over this connection. The next fix will avoid re-uploading public demo originals already present on the server, with matching schema verification.

## Export follow-up — 2026-09-16 08:44 CST

- Eliminated repeated upload of public demo originals during finalization (insurance source alone was 802 KB). Demo payloads now send a typed slug plus schema/answers; the server compares the entire schema against that exact checked-in demo before reading its PDF. Custom uploads still send their original file. Mismatched/tampered demo schemas return 400.
- Confirmed all three source PDFs appear in the Next serverless tracing manifest. No object storage or extra persistence was added.
- Acceptance: **63/63 unit/API tests**, typecheck and production build pass. All **six** complete local Chromium/mobile-WebKit demo flows pass after the change (50.1 seconds total). Deployed to https://formlilt.vercel.app (deployment `dpl_95HmpPN5XsohzckLwm2Gu1Ze47wc`). Final production regressions are running separately.

## T18 — 2026-09-16 08:50 CST — launch review assets

- Created `launch/formlilt-demo.gif` from real browser frames: homepage, insurance demo, keyboard answer, next question. `ffprobe` confirms **8.000 seconds**, **207,226 bytes**. No simulated microphone recognition or live extraction claim.
- Captured and visually inspected five 1440×1000 images: guided workspace, source explanation, attributed signature, review/download, and repository build evidence. The evidence image renders real Git commits and test metrics and explicitly identifies itself as a repository view, not an Astra chat screenshot. Capture script reports no page errors.
- Created the requested 60-second video script, Product Hunt listing/tagline, maker-comment draft, FAQ replies, build writeup and review-pack index. Included a downloadable insurance PDF with synthetic data. No PH content, contest entry, message, or launch scheduling was submitted.
- **Product-owner acceptance remains pending.** Claims distinguish the precomputed public preview from local uploads, disclose provider retention, and preserve the release gates in `QUALITY.md`.
- Final online verification after export optimization: **12/12 production tests passed in 5.1 minutes**, covering all three complete form workflows, accessibility, cached translations and local-profile behavior in both Chrome and mobile WebKit. Existing proxy and Chrome HTTP/1.1 were used for reliable transport; this does not establish production latency. Local acceptance stands at **63 unit tests**, **30 browser cases verified across relevant runs**, production builds and strict typecheck passed.

## Error-handling acceptance follow-up — 2026-09-16 09:04 CST

- Rechecked the full SPEC against current sources. Found that the upload UI classified every failure as `upload_rejected`, while model-blocked, empty-schema and finalization failures lacked translated messages. Corrected the seven specified failure-event categories, added the missing messages in all four languages, and connected turn/finalize errors to the same fixed-name event filter. Unknown codes and server messages never become analytics payloads. Hobby custom events remain disabled.
- Acceptance: **65/65 unit/API tests**, strict typecheck and production build pass. **4/4 targeted Chrome/mobile-WebKit browser tests** pass: actual PDF rendering followed by mocked timeout/blocked/empty/quota responses, retry, switching to a demo, and review editing. These checks verify failure behavior, not live extraction success.
- Verified agent-im health returns 200 and inspected its chat-completions contract: no forwarded reasoning-effort option is available. Did not add an ineffective latency setting or queue repeated long extraction calls.
- Native app automation reports a locked Mac and failed automatic unlock. Remaining Preview/VoiceOver/desktop speech checks cannot run in that state. Updated `QUALITY.md` to make the combined live-upload gate and exact launch-media gaps explicit; existing precomputed demos and repository screenshots are not evidence of those gates.
- Final homepage measurement after local-profile/error follow-ups: Lighthouse mobile **99 performance / 100 accessibility**, no run warnings (2026-09-16 01:02:53 UTC). Reports remain in `tmp/lighthouse-final.report.{json,html}`; portable metrics updated in `launch/quality-metrics.json`. Earlier gallery screenshots retain their capture-time measurements.
- Deployed tested code commit `4ee30e6` to the existing preview: Vercel deployment `dpl_8Cmj75ok8L4SvjafQzRoJXmzG9hr` is READY and aliased to https://formlilt.vercel.app. Live uploads and paid custom analytics remain disabled.
- Post-deploy targeted review regression: **2/2 passed** on production Chrome/mobile WebKit in 1.1 minutes. Verified required-field feedback, editable persisted answers, original-page rendering and narrow-screen layout on the latest deployment. The earlier 12-case full production suite remains recorded against its own deployment ID.

## Native acceptance follow-up — 2026-09-16 23:27 CST

- After the user unlocked the Mac, inspected both pages of the address and insurance exports in Preview and revisited the school export's Chinese/table/signature page (its two pages had already been inspected). All three two-page samples pass Preview appearance checks: synthetic answers and signatures fit, Chinese text is visible, and instruction pages are preserved. Recorded exact SHA-256 hashes and macOS 26.3.1 / Preview 11.0 / Chrome 152.0.7977.84 in `launch/native-verification.json`.
- Enabled real macOS VoiceOver in Accessibility settings. Completed the first five insurance questions using keyboard input: Rivera, Alex, J, 123456789 and No. Each submission advanced to the expected question and focus target. The user confirmed that question and control names were clear. Restored VoiceOver to off after this check. This is desktop evidence, not physical iPhone acceptance.
- With explicit user permission, allowed the local page microphone for this visit. Real Chrome recognition produced `one two three example Street` in the editable answer field. Corrected it to `123 Example Street` and submitted; the city question became current. No recognition mock was used for this check. The microphone stopped; no audio artifact was saved by FormLilt.
- With VoiceOver off, enabled the application's question speech. Chrome reported audio playing; the user confirmed the city question was clearly audible. Restored the app's speech toggle to off. Native UI tool timeouts required retries but did not establish application failures.
- Updated release-readiness and launch drafts to distinguish these real desktop checks from automated speech mocks and untested physical iPhone behavior. No application code changed, and automated case counts remain unchanged. Adobe Reader, real phone input, extraction speed/live-upload acceptance and production configuration remain open.

## Live-upload acceptance preparation — 2026-09-16 CST

- Revalidated the current local application and agent-im health endpoints: both return HTTP 200. Existing complete-flow tests start from precomputed demos; the upload error-path test mocks extraction. Neither proves the required combined live-upload flow.
- Added six opt-in Playwright cases in `tests/e2e/live-upload.spec.ts` (three original demo PDFs × Chromium/mobile WebKit). They use the upload input, await a real `/api/extract` response without interception, require at least 90% of the established field inventory, complete required questions and signatures, and download the PDF. Assertions cover source page count/dimensions, filled native text values, preview pages and absence of page/API errors. Responses, timings, PDF and synthetic answers are saved per run. These are intended checks, not passing results.
- `pnpm typecheck` passed; Playwright discovery lists all six cases; `git diff --check` passed. Added explicit opt-in instructions and separate local-server guidance to README so normal test runs do not consume model quota.
- Execution remains pending: two attempts to start an isolated production server on `127.0.0.1:3051` were rejected by automatic approval review with `Selected model is at capacity`. No replacement process was started and no live case was reported as passed. The existing application server remains separate. Adobe Reader installation confirmation and production model/Redis configuration also remain pending.

## Real-upload browser acceptance — 2026-09-17 00:09 CST

- The user explicitly approved the temporary local test service; it started successfully on `127.0.0.1:3051` with agent-im `codex-login/gpt-6-astra`. Ran all six browser/source combinations against the actual production build. These tests upload source PDFs and await the real extract API; they do not intercept extraction or call the precomputed demo endpoint. Separate local instances isolated the longer flows from previous quota usage.
- **5/6 passed**: Chrome address (31 fields, 155.941s upload/extraction, 198.867s total), Chrome insurance (42, 143.764s, 159.633s), mobile WebKit address (31, 106.541s, 113.855s), mobile WebKit insurance (42, 141.925s, 155.076s), Chrome school (103, 295.351s, 321.736s). Each passed required-question/signature entry, PDF download, original page count/dimensions and applicable native text-value checks. Dates are checked in their specified PDF format.
- **One genuine failure**: mobile WebKit school returned HTTP 502 `llm_error` after 302.183s including browser preparation/upload; the model's configured timeout is 300s. The failure preceded the questionnaire. The UI exposed the localized timeout and retry control. Preserved the real response, error snapshot and trace in `tmp/live-upload/webkit-school/`; no unchanged retry was queued.
- Rendered the actual successful Chrome PDFs with Poppler: inspected the filled first page of address/insurance and both school pages. Text, signatures and the school date occupy their intended areas. This check does not replace pending Adobe Reader acceptance. Portable metrics, request counts and source/output hashes are in `launch/live-upload-verification.json`; raw artifacts contain synthetic answers and remain ignored by Git.
- Typecheck and whitespace checks passed. The opt-in guard was executed separately without `RUN_LIVE_UPLOAD`: all six cases skipped, as intended. Normal unit/API and browser regression counts remain separate from this live matrix. Application code did not change, so no new deployment was needed.
- Test runner exited and its server was stopped. Verified port 3051 is closed and the existing user service on 3050 still returns HTTP 200. The full live-upload and <20s extraction gates remain open; production credentials, physical-device inputs and Reader approval are still outstanding.

## Extraction follow-up — 2026-09-17 00:42 CST

- Actual Berlin registration extraction exposed two gaps: an unconditional US date default and omission of mutually exclusive checkbox groups from the compact model response. The extraction prompt now preserves printed date formats and uses ISO dates when unspecified. The adapter also applies this default, preserves other constraints, validates checkbox group references, and adds symmetric `exclusiveWith` references consumed by existing answer validation. Old compact fixtures remain compatible.
- Added eight regression cases covering unspecified/explicit date order, retained constraints, both directions of checkbox conflicts, unrelated/false choices, and invalid group references. **73/73 unit/API tests**, strict typecheck and production build passed. Local build ID: `-4xUoHW7Gx7kg9kiOvzmc`; restarted the existing loopback-only service on 3050 with this build. The hosted preview and prior browser matrix predate these changes.
- Improved `verify-extra-forms.ts` to preserve separately tagged attempts, capture timestamps/source and export hashes, distinguish cached schemas from live extraction, fill optional fields in generic fixtures, reject blank fixtures, accept validated manual synthetic answers and return a failing exit status for failures.
- Preserved all actual model attempts. Initial Astra Berlin extraction took 166.008s but the required-only fixture exported a blank document; it does not count as acceptance. The date-only follow-up took 181.904s and returned ISO dates. The final date/group follow-up took **169.629s**, returning **55 fields**, seven ISO dates and five mutually exclusive groups. Generic answers exceeded narrow input boxes and were correctly rejected; manually selected synthetic answers use the printed `PA` code and skip inapplicable optional fields. The extracted schema was retained unchanged.
- Final German export: 46 answered fields, one original-size page, 108,809 bytes, SHA-256 `56e9451af34faa2b9630b844b3ebd34ce271cd9306e41340eda0748f93c832bb`. Checked 33 native text values and 10 checkbox values against inputs. Poppler visual review passed for names, addresses, dates, selected choices and signature. Real local `/api/turn` requests rejected conflicts and accepted false alternatives for all five groups (10 checks). The initial test setup omitted a conditional group's parent answer and received the correct 400; after activating that parent, all intended checks passed. This is live extraction plus library export/API evidence, not a browser upload end-to-end pass.
- French Cerfa also failed with Astra after **300.081s**; no unchanged retry was queued. Source hashes and attempts are recorded in `launch/extra-form-results.json`. The five-extra-PDF gate and <20s extraction target remain open. No new public deployment or push was performed; publication approval is still pending.

## Additional real PDF browser checks — 2026-09-17 01:20 CST

- Downloaded public blank Andalucía and Sterling Heights library forms and manually inventoried their user fields. The Spanish PDF has vector-outline content with only one extractable title; it exercises the scan/vision path but is not evidence of a physical paper scan. Several other candidates could not be downloaded completely; their failures are recorded, and partial files were not tested.
- The first actual Spanish upload extracted 27 fields in 120.292s. A harness selector error was found before answer entry and the run was stopped. Reviewing a cached synthetic export then exposed a real layout defect: the model combined `___ de ___ de 201_` into one full-date box, overlapping printed text. Updated the prompt to retain day/month/year-suffix blanks separately and keep a split component out of automatic signature-date linking. Also centered non-native checkbox marks within their boxes instead of drawing at the left edge.
- Added four PDF regression cases for centered checked marks and empty unchecked marks at 0/90/180/270 degrees. **77/77 unit/API tests**, strict typecheck and production build passed. Current local build: `I5gwm5Iq-zTxKttqTgiak`. Both test browser runs below use this build; earlier measurements and hosted deployment retain their own provenance.
- Corrected the signature selector and repeated the real Spanish upload because the extraction prompt and exporter changed. **Passed** in desktop Chrome: 116.537s extraction, 29/29 inventoried fields, 18 synthetic answers across 25 visible questions, typed signature, review and one-page PDF download. The unchanged original/extracted schema was used throughout. Poppler and browser PDF.js review show separate date components, centered marks and readable entries. Synthetic signing date uses the printed 201-prefix correctly as 17 September 2019. Export SHA-256: `28baf742c960e746d0bf417ff4d614fc68890e624d3b1de28424fa3b2527869e`.
- **Passed** Sterling Heights in desktop Chrome: 109.750s extraction, 25/25 inventoried user fields, 17 synthetic answers across 24 visible questions, typed signature and automatically populated local date (2026-09-17), review and one-page landscape PDF download. Parent/optional-permission/staff areas remain blank. Visual review confirms readable text, printed birthdate order, centered checkbox marks and signature placement. Export SHA-256: `931a784ac4e1025466245131706c4298bc3bf960d8df9c47a2368bd4ad9f68ec`.
- Both runs made one real extraction request, no demo requests or extraction mocks, preserved page count/dimensions, and reported no page/console/API failures. Portable evidence: `launch/extra-browser-verification.json`; raw synthetic sessions, exports, screenshots and traces remain ignored under `tmp/extra-browser/`.
- Added opt-in `scripts/verify-extra-browser.ts` and README instructions. It pauses after actual extraction for source-reviewed synthetic answers, checks their schema hash, requires an explicit answer/skip for each encountered field, prevents blank acceptance and preserves previous run directories. This test utility does not alter application schemas or bypass application routes.
- Five additional PDFs, a verified physical scan, a real phone photo, physical iPhone/Reader acceptance and extraction speed still need evidence. The original six-case live-upload matrix remains 5/6; these two additional sources do not erase its school timeout. No remote push or deployment was performed.
- Verified the browser runner's opt-in guard rejects an unflagged run before fixture reading or browser launch. Stopped the temporary service and confirmed port 3051 closed; the updated user application on 3050 returns HTTP 200. No model job remains running.

## Native widgets, dependency validation and extra-PDF acceptance — 2026-09-17 02:08 CST

- A genuine five-page GF employment scan (Lexmark X264dn, 150-DPI page images, no text layer/widgets) failed its actual Chrome upload at the configured model timeout: HTTP 502 after 300.868s. Preserved the real response and trace; no unchanged retry. Later acceptance of a simpler scan does not erase this failure.
- Tinley's native PDF contains a zero-area read-only FormsCentral appearance helper. Extraction now skips read-only fields, hidden/NoView widgets and zero-area/non-finite rectangles, while retaining visible siblings and native radio options. Clarified the prompt so office-only native fields are excluded. Added generated-PDF regression coverage. Tinley then passed actual extraction in 60.790s, all 10 user fields, signature/review/download, eight native text values and one radio value. All three office-only values remain unchanged; Poppler and browser PDF.js visual review passed.
- Castilla's first real extraction (208.826s, 51 fields) used a birthdate equality with the prose `Menor de 18 años`. A synthetic child completed the UI without ever reaching guardian fields/signature. The exported PDF confirmed the omission; this is a failed semantic acceptance despite successful transport/export checks. The original schema/session/PDF remain preserved in the archived attempt.
- Added schema validation requiring dependency equality values to pass the parent field's answer validation, with six regression cases for date/checkbox/select literals. The prompt now keeps conditions requiring computation (such as age) accessible as optional fields with actual source instructions. It does not calculate age-based obligations or invent extra fields. The extra-source browser harness now also requires every manually intended non-null answer to be reached/saved, and verifies native radio/dropdown/list values.
- After the application change, repeated the same Spanish source: 244.574s extraction, 51/51 inventoried fields, 27 synthetic answers across all 51 questions, guardian signature and separate day/month/year-suffix entries. Both original pages and the instruction page were preserved. Poppler/browser PDF.js review passed. Export SHA-256: `677b06997d107d38403b6a4314d16172be6e720f07e46dba51228f1701ccff18`.
- Indigo's one-page blank form is a physical scan (NAPS2 metadata, 2550×3300 image at 300 DPI, no text/widgets, visible paper/skew artifacts). Actual extraction took 99.178s and returned all 16 inventoried user fields. Entered 16 synthetic answers in 15 steps (signature date auto-filled), including four mutually exclusive Yes/No pairs; downloaded the original-size PDF. Both renderers show readable entries/initials/signature/date on the original scan, with staff areas blank. Export SHA-256: `3f43eed2613f2e359ef4015347964fff0c7b702c3bf02aea26f91842042abc65`.
- **Five additional real PDF flows now pass**: the prior Andalucía/Sterling runs plus Tinley, corrected Castilla and Indigo. This covers two non-English forms (both Spanish), native AcroForm, flat text and physical scan. Evidence includes per-run application/build/source/schema/export provenance in `launch/extra-browser-verification.json`. No extraction mocks, schema edits, demo endpoints or institutional submissions were used. The original three-source/two-browser live matrix remains 5/6; a real phone photo remains untested.
- **84/84 unit/API tests**, strict TypeScript and production build passed. Latest local build `dcVIDd1qONvV0YFqIG2R0`; Tinley retains its earlier build `iuBTbIWL3EJ6Eq0pveKZ9`. Two existing complete address-form regressions passed in Chrome and mobile WebKit in 12.8s; these reruns do not increase the 32 ordinary case count. After the shared-schema change, mobile Lighthouse scored **97 performance / 100 accessibility**, with no warnings. Prior 99/100 measurement is retained separately.
- Stopped the temporary test service and verified 3051 closed. Updated user service 3050 returns HTTP 200. No model/test job remains active. All fixes/evidence remain local; no push or deployment was performed. Extraction p95, the school timeout, physical phone/iPhone inputs, Reader, production model/Redis and owner launch/media acceptance remain open.


## Captioned review video — 2026-09-17 02:36 CST

- Produced `launch/formlilt-demo-draft.mp4`: exactly 60.000 seconds, H.264, 1440×1120, 25 fps, 2,373,296 bytes, with burned-in English captions and an SRT sidecar. It records the actual local production insurance demo: Chinese questions, typed fictional surname, source explanation, optional skip/return, pointer-drawn test signature, linked local date, review/download and the actual filled PDF. It contains no audio. All footage is visibly labeled as a review draft with fictional keyboard input; the omitted answer-entry/skip interval is explicitly marked as a jump cut.
- Added reproducible Playwright recording and FFmpeg editing scripts. Sessions and answers are created through real UI controls; there are no request, state or speech-result mocks. API/page errors were zero. The first recording attempt exposed a harness-only status-name mismatch (`completed`, not `done`); it was corrected before the successful capture. Raw recordings and screenshots are retained under ignored `tmp/`, with the successful source/build/export provenance in `launch/video-manifest.json`.
- Speech recording could not be completed automatically. The initial macOS-generated AIFF was empty and its padded WAV was silent; after generating a real 0.55s utterance outside the restricted shell, the padded test WAV had peak -2.6 dB and mean -24.2 dB. A headed native Chrome fake-audio-device probe and a separate speaker/microphone probe both returned native `no-speech`. No recognition output was injected. This leaves voice footage open and does not replace or invalidate the earlier human-confirmed microphone/VoiceOver/question-speech acceptance. Temporary probe browsers closed.
- Computer Use explicitly refused access to `com.openai.codex`. No alternate capture bypass was attempted. Actual Astra conversation screenshots remain pending; the existing rendered Git evidence is still not represented as conversation screenshots. Read-only inspection of agent-im's actual chat request/provider code confirmed that reasoning effort is not forwarded; no shared service configuration or unchanged long benchmark was run.
- Validation: TypeScript passed; FFmpeg decoded the whole final video without errors; final duration is 60.000s. Visually reviewed the chapter contact sheet, a full-resolution signature frame and the two-page sample's filled first page using browser PDF.js and Poppler. Names, member-number test value, signature and date are readable in their boxes; output is two original-size 612×792pt pages. Application code did not change, so previous 84-unit-test and browser suite counts remain unchanged.
- Updated launch README/script/listing/writeup and release-readiness wording. The exact live-upload/voice scenario, recorded question speech, actual conversation captures and owner acceptance remain open. No model extraction, public upload, remote Git push, deployment or launch submission was performed in this follow-up.


## Scan instructions and field-type follow-up — 2026-09-17 02:58 CST

- Ran one isolated model-only probe through the existing agent-im API using `codex-login/gpt-5.6-sol` on the unchanged public Tinley dog-license PDF. It returned all 10 applicant field identities/native references in **59.467s**, one successful call, no repair; adapter-reported usage was 22,566 input and 758 output tokens. This is direct extraction timing, not a browser run or a p95 measurement. It does not establish the <20s target, so the application's Astra default was retained. Portable evidence: `launch/extraction-latency-probes.json`.
- Found a specification gap: compact extraction could only reference existing PDF text-item indices for field help, so pure scans/images could not carry readable printed instructions into the explanation UI. Added optional bounded `[rowIndex, sourceQuote]` entries for scan pages. Each quote is limited to 300 characters; unknown/duplicate row references, text-page use and conflicting indexed/visual help are rejected. Prompt requires exact visible, same-page, original-language excerpts and omission when unreadable. Older compact responses remain compatible.
- Corrected grounding for mixed PDFs: an excerpt from a scan page now survives even when another page has a text layer. Text-page help still requires source-string evidence. Added nine regression/API cases covering PDF/image scans, mixed pages, invalid references, conflicting help, absent instructions, and source-quote delivery without model-generated advice. Six counterexamples failed before the fix. An initially invalid scan API fixture lacked its required bbox; corrected the fixture without weakening validation. **93/93 unit/API tests**, strict TypeScript and production builds passed.
- First real Chrome scan follow-up: **103.641s**, 18 returned fields, exact source instructions displayed in the explanation UI. Manual review then found four underlined parenthesis writing blanks inferred as checkboxes. The diagnostic flow skipped those uncertain optional blanks, filled 12 other fields and exported; it is explicitly **not accepted as complete semantic coverage**. Its response, screenshots, trace and partial PDF are preserved.
- Clarified the prompt to preserve parenthesized writing/initials blanks as short text unless the source explicitly directs a tick. This general rule separates those blanks from nearby Yes/No choices. Rebuilt and repeated the same actual source because the prompt changed. Corrected run: **104.226s**, all **16 applicant fields** plus two optional staff fields; four initials fields now accept `AR`. Entered 16 synthetic answers over 17 UI steps (signed date auto-filled), displayed the exact reviewed source quote, signed as Alex Rivera, left staff fields blank and downloaded the original-size one-page PDF. Export SHA-256: `86b2e627e407a8fc314aa523b6a8915281e104700dfb774648e083d51ee8324f`.
- Poppler and browser PDF.js visual review passed: names/date, four initials, four mutually exclusive No marks, patient signature/local date occupy the intended areas; staff regions remain blank. All distinct extracted quotations match the original scan. There were no page/console/API failures or extraction/demo mocks. This repeats Indigo and does not increase the five-extra-PDF count. Evidence with both attempts, source/build/schema/output hashes and implementation file hashes: `launch/scan-help-verification.json`.
- Existing invalid-email/source-explanation regressions passed in Chrome and mobile WebKit, **2/2 in 4.0s**, on the first scan-help build `g2kaaSrBF18XRn48XM41u`. Final prompt correction uses build `vSj_nJzB_54-euLHae2aN` and passed the real scan workflow. UI code did not change; earlier Lighthouse measurements retain their original build provenance. Added optional source-help UI assertions to the extra-form harness and documented that text equality does not replace visual OCR verification.
- Temporary service 3051 stopped after verification; user application 3050 uses the new build. No model job remains running, default/provider service configuration was not changed, and no remote push/deployment/submission was performed. Extraction latency, original WebKit school timeout, physical phone/iPhone, Reader, production credentials, remaining launch media and owner acceptance remain open.


## Remaining-gate audit and documentation — 2026-09-17 CST

- Reconciled README with the authoritative 93-test scan-help results and retained Lighthouse's actual measurement build. Clarified that staff signatures can remain optional rather than claiming every staff input is excluded. No application or acceptance scope changed.
- Read-only configuration check confirms the local provider remains loopback agent-im/Astra; local OpenAI/Ark keys and Upstash URL/token are absent. Adobe Reader remains uninstalled. No physical iPhone/photo input or owner release acceptance was supplied. The known extraction latency, hosted transport/analytics constraints and remaining media requirements therefore remain open; no unchanged benchmark or unsupported device acceptance was substituted.
- Validation: documentation compared against `launch/quality-metrics.json` and `launch/scan-help-verification.json`; `git diff --check` passed. No test rerun was needed for this documentation-only correction.

## T33 — 2026-09-17 — Owner-authorized Vercel live deployment

- Owner selected Vercel, the existing system Chrome for post-deployment acceptance, and Doubao `doubao-seed-2-0-pro-260215` through the public Ark endpoint. Owner explicitly accepted basic per-instance limits without Redis and authorized GitHub creation/push and deployment. Existing repository/project are reused.
- Added explicit `RATE_LIMIT_MODE=memory` opt-in; shared mode still fails closed. Updated privacy copy in all four languages to accurately describe hashed-IP counters and non-global memory limits.
- Ark requests explicitly disable thinking for bounded JSON output; images and schema validation remain in the existing pipeline. A real blank-form image/JSON smoke returned HTTP 200 in 2.977s with the correct title and no reasoning tokens. This small smoke is not an extraction benchmark.
- API key saved only in ignored `.env.local` (mode 0600) and Vercel Sensitive production configuration via stdin. It is not printed or stored in source.
- Enabled Fluid Compute with a 300s extraction route and 280s model timeout. No plan upgrade or Redis subscription. Unit/API tests 95/95, strict typecheck and production build passed. Full Ark extraction and deployed Chrome acceptance follow below.

- Full Ark extraction checks exposed a semantic index mismatch: a schema could validate while targeting another native PDF field. The prompt now includes source widget names and explicitly preserves original widget indices when omitting office fields. The corrected 11.073s local run maps all 10 applicant fields to their corresponding native widgets. Three extra office fields remain optional and are skipped in acceptance. Earlier 280s timeout, schema-validation failures, and index mismatch are preserved in `launch/ark-integration-verification.json`; none count as passed.

## T34 — 2026-09-17 — Production integration and remaining Chrome unlock

- Published all authorized source to `DanielMax937/formlilt`. Current application deployment is `dpl_7vHkwxt75F7YJ5oaHtTrT8t52nCH`, source `4ce694f8bef962ba5cac2bfeda4405856ac25cdc`, aliased to https://formlilt.vercel.app.
- Found a production-only PDF bug: `field.constructor.name` was minified by Next. Replaced it with class-identity checks; regression simulates a renamed radio class. All 96 tests and production build pass. Native radio choices now retain the correct type.
- Added one bounded Ark transport retry and safe diagnostics containing only error names/codes/status. No request/response body, headers, model input, key or error message is logged. Hong Kong still failed before HTTP responses; moved functions to the spec-permitted Singapore region without changing the Ark endpoint/model or Vercel plan.
- Singapore production: Tinley extraction 12.820s; Chinese `金色` translated to `golden` in 4.623s with confirmation; PDF export HTTP 200. All 10 native values and the selected Female radio value verified. Poppler visual review confirmed readable text/signature and blank office fields. The test checker initially compared the ISO date input against a formatted PDF value; corrected the checker to use the schema's date format, without changing the app/schema. This is one successful source, not p95 evidence. Optional model summary exceeded its 1.8s budget; deterministic fallback preserved the export.
- Public safety check scanned 452 Git-history blobs and 12 served client scripts plus HTML: the supplied API key was absent. `.env.local` remains ignored with mode 0600; Vercel stores the key as Sensitive. About shows the selected model and per-instance limit disclosure. Hosted upload copy now says the prepared request limit is 4.4 MB instead of promising 10 MB.
- System Chrome native UI connection succeeded after the tab connector timed out. An attempted Apple Events JavaScript setting change was rejected by auto-review; it was never enabled. Native UI works without it, and the related optional approval was explicitly withdrawn in commentary. The user-facing Chrome home showed live upload controls. The Mac then locked at file selection; requested unlock and left full browser acceptance pending. HTTP checks are not reported as native browser acceptance.
- Portable evidence: `launch/ark-production-verification.json`; synthetic output: `launch/ark-production-filled.pdf`. Earlier local/provider failures remain in `launch/ark-integration-verification.json` and temporary diagnostics.
